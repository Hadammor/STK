# Copy this to .env.local and paste your Mapbox public token.
# Get a free token at https://www.mapbox.com (Account → Tokens → default public token).
# Without a token the app still runs and shows a styled fallback map.
VITE_MAPBOX_TOKEN=pk.xxxx
